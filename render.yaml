services:
  - type: web
    name: myweb
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn myweb.wsgi
    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: myweb.settings